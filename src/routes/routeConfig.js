export const url = "localhost:3001";

export const signUpUrl = `${url}/signup`;
export const signInUrl = `${url}/signin`;

export const getClients = `${url}/getClients`;
export const addClient = `${url}/addClient`;

export const getAllDevices = `${url}/getAllDevices`;
export const getDevicesByClient = `${url}/getDevicesByClient`;
export const addDevice = `${url}/addDevice`;
export const addDeviceToClient = `${url}/addDeviceToClient`;
